'use strict';

var DomWalker = require('./dom_walker');

/**
 * @constructor
 */
function TextUtilities() {
}

(function() {

  /**
   *
   * @param {Node} node
   * @param {number} startOffset
   * @returns {number}
   */
  TextUtilities.offsetFrom = function(node, startOffset) {

  };

}).call(TextUtilities);

module.exports = TextUtilities;
